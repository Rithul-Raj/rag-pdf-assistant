# Core dependencies
pymupdf>=1.24.0               # PDF parsing (imports as fitz)
# langchain-text-splitters removed — we use a pure-Python recursive splitter
# (langchain-core triggers LangSmith telemetry on import, causing network hangs)
sentence-transformers>=3.0.0  # Local embeddings (all-MiniLM-L6-v2)
chromadb>=0.5.0               # Vector database (local, persists to disk)
rank-bm25>=0.2.2              # BM25 sparse retrieval
streamlit>=1.35.0             # Web UI
groq>=0.9.0                   # Groq API client (free tier LLM)
python-dotenv>=1.0.0          # Load .env into os.environ

# Evaluation
ragas>=0.1.14                 # RAG evaluation metrics (faithfulness, etc.)

# Dev / testing
pytest>=8.0.0
pytest-cov>=5.0.0             # Coverage reports
